Create engagement APIs.

Endpoints:

POST /posts/:id/like
POST /posts/:id/comment
POST /posts/:id/bookmark

Return updated counts after each action.

Send notifications to post owners on like and comment.